import { View, Text } from "react-native";

export default function SmokeTest() {
  return (
    <View className="flex-1 items-center justify-center bg-sky-200">
      <Text className="text-xl font-bold text-sky-800">
        🟢 NativeWind works on Web & iOS!
      </Text>
    </View>
  );
}
